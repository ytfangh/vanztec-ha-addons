build_from:
  amd64: ghcr.io/home-assistant/amd64-base-debian:bookworm
  aarch64: ghcr.io/home-assistant/aarch64-base:3.19

args:
  # 直接指向新仓库
  RATHOLE_OWNER: "rathole-org"    # 改为新组织
  RATHOLE_REPO: "rathole"         # 仓库名不变
  RATHOLE_TAG: "v0.5.0"           # 标签格式正确
